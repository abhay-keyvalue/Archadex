import React from "react";
import { CreateProjectScreen } from "../containers";

const CreateProjectPage: React.FC = () => (
  <div className="h-full">
    <CreateProjectScreen />
  </div>
);

export default CreateProjectPage;
